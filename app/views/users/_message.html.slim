- if conversation(user).present?
  = link_to t('buttons.message'), conversation_path(conversation(user)), class: 'btn btn-primary btn-sm'
- else
  = link_to t('buttons.message'), user_conversations_path(user), method: :post, class: 'btn btn-primary btn-sm'

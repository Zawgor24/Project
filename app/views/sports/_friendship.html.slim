- if current_user.follows?(@sport)
  = button_to 'Unfollow', sport_follows_path(@sport), method: :delete, class: 'btn btn-default'
- else
  = button_to 'Follow', sport_follows_path(@sport), class: 'btn btn-primary'
